#include <stdio.h>
/**
 * main - main block
 * Description: Use `putchar` to print all letters but the letter 'q' and 'e'.
 * Return: 0
 */
int main(void)
{
	int i;

	for (i=0; i<=9'; i++)
	{
		printf("%i", i);
		c++;
	}
	putchar('\n');

	return (0);
}
