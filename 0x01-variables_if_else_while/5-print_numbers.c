#include <stdio.h>
/**
 * main - main block
 * Description: Use `printf` to print all one digit numebrs starting from 0.
 * Return: 0
 */
int main(void)
{
	int i;

	for (i=0; i<10'; i++)
	{
	printf("%i", i);
		c++;
	}
	putchar('\n');

	return (0);
}
